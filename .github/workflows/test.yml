name: CI
on: 
  push: 
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Git checkout
        uses: actions/checkout@v1
      - name: Branch name
        run: echo running on branch ${{ github.action_repository }} && echo running on branch ${{ github.head_ref }}

